version: v1alpha1
metadata:
  name: btrfs
  version: "{{ .VERSION }}"
  author: Enno Boland
  description: |
    [{{ .TIER }}] This system extension provides kernel module driver and userspace tools for BTRFS built against a specific Talos version.
  compatibility:
    talos:
      version: ">= v1.5.0"
