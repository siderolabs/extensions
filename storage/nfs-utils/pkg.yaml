name: nfs-utils
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - stage: libtirpc-zfs
    from: /rootfs
  - stage: rpcbind
    from: /rootfs
  - stage: nfs-utils-statd
    from: /rootfs
steps:
  - install:
    - |
      mkdir -p /rootfs/usr/local/lib/containers/rpcbind
      mkdir -p /rootfs/usr/local/lib/containers/rpc-statd

      # cleanup
      rm -rf /rootfs/usr/local/include
      rm -rf /rootfs/usr/share
    - |
      mkdir -p /rootfs/usr/local/etc/containers

      cp /pkg/rpcbind.yaml /rootfs/usr/local/etc/containers/rpcbind.yaml
      cp /pkg/rpc-statd.yaml /rootfs/usr/local/etc/containers/rpc-statd.yaml
    - |
      # Create necessary state directories
      mkdir -p /rootfs/var/lib/nfs/statd
      mkdir -p /rootfs/var/lib/nfs/statd/sm
      mkdir -p /rootfs/var/lib/nfs/statd/sm.bak
      mkdir -p /rootfs/var/lib/rpcbind
      mkdir -p /rootfs/run/rpcbind
    test:
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
    sbom:
      outputPath: /rootfs/usr/local/share/spdx/nfs-utils.spdx.json
      version: {{ .VERSION }}
      licenses:
        - BSD-3-Clause
        - GPL-2.0
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /
