version: v1alpha1
metadata:
  name: gvisor
  version: "{{ .VERSION }}"
  author: Sidero Labs
  description: |
    [{{ .TIER }}] This system extension provides gVisor using containerd's runtime handler.
  compatibility:
    talos:
      version: ">= v1.0.0"
