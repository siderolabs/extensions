version: v1alpha1
metadata:
  name: amdgpu
  version: "{{ .VERSION }}"
  author: Sidero Labs
  description: |
    [{{ .TIER }}] This system extension provides AMDGPU firmware binaries and kernel modules.
  compatibility:
    talos:
      version: ">= v1.0.0"
