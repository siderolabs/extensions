version: v1alpha1
metadata:
  name: xen-guest-agent
  version: "{{ .VERSION }}"
  author: Jerwin NJ
  description: |
    [{{ .TIER }}] xen-guest-agent communicates information and metrics with the Xen host.
  compatibility:
    talos:
      version: ">= v1.5.0"
