version: v1alpha1
metadata:
  name: applesmc
  version: "$VERSION"
  author: Steven Kreitzer
  description: |
    This system extension provides applesmc drivers kernel modules built against a specific Talos version.
  compatibility:
    talos:
      version: ">= v1.5.0"
