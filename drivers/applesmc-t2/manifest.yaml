version: v1alpha1
metadata:
  name: applesmc-t2
  version: "$VERSION"
  author: Anthony A.
  description: |
    applesmc module patched for t2 chips to allow fan control with t2fand
  compatibility:
    talos:
      version: ">= v1.5.0"
