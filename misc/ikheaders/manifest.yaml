version: v1alpha1
metadata:
  name: ikheaders
  version: "$VERSION"
  author: SideroLabs
  description: |
    This system extension provides the Linux kernel headers module.
  compatibility:
    talos:
      version: ">= v1.5.0"
