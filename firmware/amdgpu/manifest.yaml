version: v1alpha1
metadata:
  name: amdgpu
  version: "$VERSION"
  author: Antony Rabbito
  description: |
    This system extension provides AMD GPU firmware for enablement.
  compatibility:
    talos:
      version: ">= v1.0.0"
